export interface Pet {
  readonly name: string;
  readonly code: number;
  readonly type: any; // TODO: replace with enum value
  readonly furColor: any; // TODO: replace with enum value
  readonly country: any; // TODO: replace with enum value
}
